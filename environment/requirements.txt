# DeShortcut-Align training environment (Python 3.10, CUDA 12.1 recommended)
# Usage:
#   conda create -n deshortcut python=3.10 -y && conda activate deshortcut
#   pip install -r environment/requirements.txt
#   pip install -e verl/
#
# Notes:
# - Install torch/torchvision from the CUDA wheel index first if the default CPU build is picked.
# - flash-attn often needs a matching wheel or: pip install flash-attn --no-build-isolation
# - Optional pin file: requirements-verl-freeze.clean.txt

# --- core deep learning (torch 2.4.0 + cu121) ---
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.0
torchvision==0.19.0
triton==3.0.0

# --- inference / RL stack ---
vllm==0.6.3
ray[default]==2.47.1
transformers==4.57.3
accelerate==1.12.0
peft==0.18.1
datasets==4.4.2
tensordict>=0.8.0,<=0.10.0,!=0.9.0
torchdata==0.11.0

# --- verl / training utils ---
codetiming
dill
hydra-core==1.3.2
numpy<2.0.0
pandas
pyarrow>=19.0.0
pybind11
pylatexenc
packaging>=20.0
uvicorn
fastapi
latex2sympy2_extended
math_verify
tensorboard
wandb
swanlab==0.7.6

# --- reward / API ---
openai>=1.0.0
omegaconf==2.3.0

# flash-attn: install separately after torch (often needs a matching wheel)
#   pip install flash-attn==2.7.3 --no-build-isolation
